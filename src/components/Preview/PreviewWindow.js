import React from "react";
import styled from "styled-components";

const StyledPreview = styled``;

const PreviewWindow = () => {
  return <StyledPreview></StyledPreview>;
};

export default PreviewWindow;
